# Local benchmark downloads

PDF files in this directory are intentionally ignored by Git. They remain local source material; the repository tracks only their provenance, license, expected hash after acquisition, and selected page windows in `../sources.json`.

The network and distributed-systems PDFs may be downloaded from their recorded source URLs. The CUNY repository currently blocks automated retrieval, so acquire that chapter through its public landing page if needed.
